plugins {
  id 'org.eduardoleolim.kotlin-library-conventions'
}

ext {
  moduleName = 'org.eduardoleolim.organizadorpec660.shared'
}

compileJava {
  options.compilerArgs.addAll(['--patch-module', "${project.moduleName}=${sourceSets.main.output.asPath}"])
}

kotlin {
  jvmToolchain(17)
}
